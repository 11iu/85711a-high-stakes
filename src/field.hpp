/*
 * Field locations for auto
 * Units are inches and degrees
 * Orientation according to field.png, blue team on right side
 */

/*
struct Pose2d
{
    float x;
    float y;
    float angle;

    Pose2d(float xVal, float yVal, float angleVal)
    {
        x = xVal;
        y = yVal;
        angle = angleVal;
    }
};

struct Translation2d
{
    float x;
    float y;

    Translation2d(float xVal, float yVal)
    {
        x = xVal;
        y = yVal;
    }
};

float tile = 24;
float fieldX = 144;
float fieldY = 144;
float ringOD = 7;
float ringID = 3;
float ringThick = 2;

Translation2d origin = Translation2d(0, 0);

// rings, starting top ring color
Translation2d ringRedMiddleUpper = Translation2d(ringOD / 2, ringOD / 2);
Translation2d ringRedMiddleLower = Translation2d(ringOD / 2, -ringOD / 2);
Translation2d ringBlueMiddleUpper = Translation2d(-ringOD / 2, ringOD / 2);
Translation2d ringBlueMiddleLower = Translation2d(-ringOD / 2, -ringOD / 2);
Translation2d ringRedUpperUpper = Translation2d(ringOD / 2, tile * 2 + ringOD / 2);
Translation2d ringRedUpperLower = Translation2d(ringOD / 2, tile * 2 - ringOD / 2);
Translation2d ringBlueUpperUpper = Translation2d(-ringOD / 2, tile * 2 + ringOD / 2);
Translation2d ringBlueUpperLower = Translation2d(-ringOD / 2, tile * 2 - ringOD / 2);
Translation2d ringRedLowerInner = Translation2d(tile, -tile * 2);
Translation2d ringRedLowerOuter = Translation2d(tile * 2, -tile * 2);
Translation2d ringBlueLowerInner = Translation2d(-tile, -tile * 2);
Translation2d ringBlueLowerOuter = Translation2d(-tile * 2, -tile * 2);
Translation2d ringRedLeft = Translation2d(-tile * 2, 0);
Translation2d ringBlueRight = Translation2d(tile * 2, 0);
Translation2d ringRedRight = Translation2d(tile * 2.5, 0);
Translation2d ringBlueLeft = Translation2d(-tile * 2.5, 0);
Translation2d ringRedUpperOuter = Translation2d(tile, tile * 2);
Translation2d ringBlueUpperOuter = Translation2d(-tile, tile * 2);
Translation2d ringRedRightCorner = Translation2d(tile * 3 - ringOD / 2, -tile * 3 + ringOD / 2);
Translation2d ringRedLeftCorner = Translation2d(-tile * 3 + ringOD / 2, -tile * 3 + ringOD / 2);
Translation2d ringBlueRightCorner = Translation2d(tile * 3 - ringOD / 2, tile * 3 - ringOD / 2);
Translation2d ringBlueLeftCorner = Translation2d(-tile * 3 + ringOD / 2, tile * 3 - ringOD / 2);

// goals
Translation2d goalCenterLower = Translation2d(0, -tile * 2);
Translation2d goalRightLower = Translation2d(tile, -tile);
Translation2d goalRightUpper = Translation2d(tile, tile);
Translation2d goalLeftLower = Translation2d(-tile, -tile);
Translation2d goalLeftUpper = Translation2d(-tile, tile);
*/
